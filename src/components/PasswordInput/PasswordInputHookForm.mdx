import { Meta, Canvas, ArgTypes } from "@storybook/blocks"
import * as Stories from "./PasswordInputHookForm.stories"

<Meta name="HookForm" of={Stories} />

# \<PasswordInput />

PasswordInput is a `react-hook-form` support version of PasswordInput. \
This component simply extends `<input type="password" />` with styles

<Canvas of={Stories.HookForm} />

### Usage

```tsx
import { useForm, FormProvider } from "react-hook-form"
import { PasswordInput } from "@reactleaf/input/hookform"

function FormComponent() {
  const form = useForm()

  return (
    <FormProvider {...form}>
      <PasswordInput name="name" label="Name" />
    </FormProvider>
  )
}
```

### Extended Props

This components extended from raw component. Just add some props for `<Controller />` such as `name` or `rules`

- `value` - is excluded. `value` is handled by `react-hook-form`.

<ArgTypes of={Stories.HookForm} include={["name", "label", "clearable", "onValueChange", "onEnter"]} />
