import { Canvas, Meta, Controls } from "@storybook/blocks"
import * as NumberInputStories from "./NumberInput.stories"

<Meta of={NumberInputStories} />

# \<NumberInput />

NumberInput is a special input to handle number input.

<Canvas of={NumberInputStories.Default} />
<Canvas of={NumberInputStories.Formatter} />

### Use in `react-hook-form`

`onChange` in `<NumberInput />` will return formatted `string` value, \
so using `register()` is not recommended in this component.

```tsx
import { TextInput } from "@reactleaf/input"

function FormComponent() {
  const { control } = useForm()
  return (
    <>
      {/* using <Controller /> is recommended */}
      <Controller
        name="withFormatter"
        control={control}
        render={({ field: { value, onChange, ...field }, fieldState }) => (
          <NumberInput
            label="WithFormatter"
            {...field}
            formatter={customFormat.formatter}
            max={999_999_999}
            parser={customFormat.parser}
            onValueChange={onChange}
            errorMessage={fieldState.error.message}
          />
        )}
      />
    </>
  )
}
```

## Props

`<NumberInput />` extends almost all props of `<input />` like `<TextInput />`, but excetps, `type`.

> **WARNING** \
> `@reactleaf/input` extends basic `<input />`, so `onChange` recieves `event` as a parameter. \
> Reading `event.currentTarget.value` from event listeners would be a text value, not a number. \
> If you want to get numeric value, use `onValueChange` instead.

<Controls
  of={NumberInputStories.Default}
  include={["label", "clearable", "errorMessage", "step", "onValueChange", "onEnter", "formatter", "parser"]}
/>
